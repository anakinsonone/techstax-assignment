To install the project dependencies for the front-end and the back-end, use:

```
npm install
```

To run the front-end locally, use:

```
cd frontend/
npm run dev
```

To run the back-end locally, use:

```
cd backend/
npm run dev
```
